class ChatCommand {
	static get command() {
		return '';
	}

	static get helpMessage() { return 'Help message not defined.'; }

	static get helpInfo() { return 'Help info not specified.'; }

	static invoke(manager, args) {}
}